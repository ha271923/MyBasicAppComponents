package sample.hawk.com.mybasicappcomponents.utils;

// import android.os.SystemProperties;


// Can only use it if APP signed platform key.
public class SystemPropertiesWrapper {
    /*
    public static String get(String id, String defaultValue) {
        return SystemProperties.get(id, defaultValue);
    }

    public static int getInt(String id, int defaultValue) {
        return SystemProperties.getInt(id, defaultValue);
    }

    public static boolean getBoolean(String id, boolean defaultValue) {
        return SystemProperties.getBoolean(id, defaultValue);
    }
    */
}